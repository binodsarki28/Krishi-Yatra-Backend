spring:
  application:
    name: KrishiYatra

  datasource:
    mysql:
      url: jdbc:mysql://localhost:3306/krishi-yatra
      username: root
      password: WJ28@krphs
    # Default properties for Spring (though overridden by DataSourceConfig)
    url: jdbc:h2:mem:krishi_yatra;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:/db/stub-migration
    baseline-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: onlycoding6969@gmail.com
    password: "dzwg ttjn dbst knib"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  api-docs:
    version: openapi_3_0

app:
  otp:
    expiration-minutes: 5
    max-attempts: 5