spring:
  application:
    name: KrishiYatra

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/Krishi-Yatra}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:WJ28@krhps}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/stub-migration/{vendor}
    baseline-on-migrate: true
    out-of-order: true
    validate-on-migrate: true
    repair-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: onlycoding6969@gmail.com
    password: "dzwg ttjn dbst knib"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  api-docs:
    version: openapi_3_0

app:
  otp:
    expiration-minutes: 5
    max-attempts: 5

cloudinary:
  cloud-name: dcffdurtb
  api-key: "448551618111328"
  api-secret: PTnYks9WoHTcMTjpntnVchqOwcc