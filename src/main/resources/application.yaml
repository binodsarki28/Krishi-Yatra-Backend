spring:
  application:
    name: KrishiYatra

  datasource:
    mysql:
      url: jdbc:mysql://localhost:3306/krishi-yatra
      username: root
      password: "WJ28@krhps" #
    url: jdbc:h2:mem:krishi_yatra;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    out-of-order: true
    validate-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: onlycoding6969@gmail.com
    password: "dzwg ttjn dbst knib"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  api-docs:
    version: openapi_3_0

app:
  otp:
    expiration-minutes: 5
    max-attempts: 5